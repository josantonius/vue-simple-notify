os:
  - linux

sudo: false

language: node_js

node_js:
  - "6"

before_install:
  - npm install npm@latest -g

script:
  - npm run lint
  - npm run unit
  - npm run bundle
  - npm run build

after_success:
  - bash <(curl -s https://codecov.io/bash)
